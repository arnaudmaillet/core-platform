# Per-service Redis cache for social-graph (env: REDIS_HOSTS=dev-social-graph-redis:6379).
# Ephemeral (emptyDir) — these hold regenerable hot-cache state. The overlay's
# namePrefix turns the name into dev-social-graph-redis; the `app` label is not prefixed,
# so the Service selector still matches.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-graph-redis
  labels:
    app: social-graph-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-graph-redis
  template:
    metadata:
      labels:
        app: social-graph-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 3
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: social-graph-redis
  labels:
    app: social-graph-redis
spec:
  selector:
    app: social-graph-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
