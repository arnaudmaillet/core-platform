# Per-service Redis cache for timeline (env: REDIS_HOSTS=dev-timeline-redis:6379).
# Ephemeral (emptyDir) — these hold regenerable hot-cache state. The overlay's
# namePrefix turns the name into dev-timeline-redis; the `app` label is not prefixed,
# so the Service selector still matches.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: timeline-redis
  labels:
    app: timeline-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timeline-redis
  template:
    metadata:
      labels:
        app: timeline-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 3
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: timeline-redis
  labels:
    app: timeline-redis
spec:
  selector:
    app: timeline-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
