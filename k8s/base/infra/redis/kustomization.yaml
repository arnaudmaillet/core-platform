# Per-service Redis caches (dev). namePrefix in the dev overlay -> dev-<svc>-redis.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - chat.yaml
  - engagement.yaml
  - geo-discovery.yaml
  - notification.yaml
  - profile.yaml
  - social-graph.yaml
  - timeline.yaml
