# k8s/base/services/media/worker/service.yaml
#
# Headless service: a worker takes no inbound traffic, but a stable endpoint lets
# the health/reflection plane be scraped (probes, metrics, grpc_health_probe).
apiVersion: v1
kind: Service
metadata:
  name: media-worker
  labels:
    app: media-worker
spec:
  clusterIP: None
  selector:
    app: media-worker
  ports:
    - name: grpc
      protocol: TCP
      port: 50071
      targetPort: grpc
      appProtocol: grpc
