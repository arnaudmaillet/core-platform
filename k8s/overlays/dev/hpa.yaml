# k8s/overlays/dev/hpa.yaml

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: profile-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dev-profile-server
  minReplicas: 2 # On commence à 2 pour tester le balancing
  maxReplicas: 10 # Limite haute pour forcer Karpenter à créer des Nodes
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 10 # On scale si le CPU dépasse 10% de la "request"