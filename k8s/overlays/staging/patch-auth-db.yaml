# auth-server: DATABASE_URL (CNPG uri) into the migrator init container + server.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-server
spec:
  template:
    spec:
      initContainers:
        - name: migrate-auth
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: staging-auth-postgres-app
                  key: uri
      containers:
        - name: server
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: staging-auth-postgres-app
                  key: uri
