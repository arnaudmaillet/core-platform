# infrastructure/argocd/apps/infrastructure/platform/karpenter-config/templates/nodepools.yaml

{{- range $name, $config := .Values.nodepools }}
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: {{ $name }}
spec:
  template:
    spec:
      nodeClassRef:
        group: karpenter.k8s.aws
        kind: EC2NodeClass
        name: default
      requirements:
        - key: "karpenter.sh/capacity-type"
          operator: In
          values: {{ $config.capacityType | toJson }}
        - key: "karpenter.k8s.aws/instance-category"
          operator: In
          values: {{ $config.categories | toJson }}
        - key: "karpenter.k8s.aws/instance-size"
          operator: In
          values: {{ $config.sizes | toJson }}
        # Ajout du label d'intention pour le ciblage
        - key: "intent"
          operator: In
          values: [{{ $name | quote }}]
      
      # On ajoute une Taint uniquement pour le pool database
      {{- if eq $name "database" }}
      taints:
        - key: "dedicated"
          value: "database"
          effect: "NoSchedule"
      {{- end }}

  limits:
    cpu: {{ $config.cpuLimit }}
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 1m
---
{{- end }}